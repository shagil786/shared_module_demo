{
  "devDependencies": {
    "esbuild": "^0.23.1"
  },
  "scripts": {
    "build": "esbuild src/Footer/index.jsx --bundle --outfile=dist/shared-footer.js --external:react --external:react-dom --format=esm"
  }
}
